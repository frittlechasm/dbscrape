# Repository Guidelines

- Keep changes small and focused.
- Preserve the current CLI and `/tmp` output behavior unless asked otherwise.
- Prefer simple Bash and existing tools already used by `dbscrape`.
- Run `bash -n dbscrape` after shell changes.
- Update `README.md` if behavior or usage changes.
